# Day 003：函数、控制流与边界

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-002.md) · [下一天](day-004.md)

前置：完成 [Day 2](day-002.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

默认参数在函数定义时求值，所以可变列表不适合直接当默认值。关键字参数让调用意图清晰，推导式适合简单筛选，不适合塞复杂业务。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 在练习文件准备 20 台含 normal、warning、critical 的设备

2. 实现 filter_alarm_devices，使用 get 处理缺失 level，默认筛 critical

3. 用空列表、全命中、无命中、缺失字段、自定义等级五组 assert 检查

## 观察结果与验收（10 分钟）

输入列表不被修改；输出只含匹配设备；能手写普通 for 版本再改为推导式。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

写一个 items=[] 默认参数函数并连续调用两次，观察值残留；改成 None 后在函数内建列表。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

如果要校验 level 合法性，放在筛选函数还是边界模型？说明选择而非只说都可以。

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-003.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-003: 函数、控制流与边界`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-1)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
