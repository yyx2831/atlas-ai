# Day 146：离线依赖清单

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-145.md) · [下一天](day-147.md)

前置：完成 [Day 145](day-145.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

断网交付不仅需要镜像，还需要驱动前置、模型、前端、配置、证书信任与完整软件包依赖。下载地址不能替代实际文件。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 列出操作系统、CPU 架构、GPU、镜像 digest、Python 版本、wheel、模型 revision 与配置模板

2. 将在线构建和离线运行分成两阶段

3. 给每项生成来源、许可、大小、校验和与安装顺序

## 观察结果与验收（10 分钟）

清单能对应实际文件，未打包的前置依赖明确说明。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

用禁止外网的测试网络运行安装，记录每一个意外访问地址并补依赖。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

为何离线环境的系统架构与 Python ABI 必须在打包前确认？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-146.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-146: 离线依赖清单`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-5)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
