# Day 177：本地集群运行 FastAPI

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-176.md) · [下一天](day-178.md)

前置：完成 [Day 176](day-176.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

先部署一个无数据库健康端点，隔离集群和业务复杂度。镜像必须让节点可访问，本机 docker 里有不代表 kind 节点里已有。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 按官方流程创建 kind 或 minikube 测试集群

2. 构建无外部依赖的 lab API，按手册导入 kind 镜像并 apply Deployment/Service

3. 用 port-forward 访问 /health，检查 get pods 与 logs

## 配套示例

打开 [关键实验手册](../labs/key-recipes.md#day-177)，按本日编号使用示例。

## 观察结果与验收（10 分钟）

Pod Ready，健康接口返回成功；不公开集群管理 API，不部署真实客户数据。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

使用不存在镜像 tag 触发 ImagePullBackOff，导入正确镜像或修正 tag 后恢复。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

本地镜像导入与远端 registry 拉取有什么区别？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-177.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-177: 本地集群运行 FastAPI`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-6)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
