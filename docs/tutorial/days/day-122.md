# Day 122：Prometheus 指标

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-121.md) · [下一天](day-123.md)

前置：完成 [Day 121](day-121.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

Counter 表示累计事件，Gauge 表示当前值，Histogram 表示分布。标签形成独立时间序列，设备 ID 或任意 URL 会造成基数爆炸。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 按手册增加请求 Counter 与延迟 Histogram

2. 只用方法、路由模板与有限状态标签，不用完整动态路径

3. 保护 /metrics，仅供监控网络抓取

## 配套示例

打开 [关键实验手册](../labs/key-recipes.md#day-122)，按本日编号使用示例。

## 观察结果与验收（10 分钟）

执行已知数量请求后计数变化一致，延迟单位为秒，动态 ID 不生成无限序列。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

将真实 URL 作为标签做小规模反例，观察序列增多，改回 /devices/{id} 模板。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

多 worker 的指标采集为什么不能假设一个进程的内存计数代表全部实例？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-122.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-122: Prometheus 指标`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-5)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
