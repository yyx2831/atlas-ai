# Day 144：低 GPU 利用率

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-143.md) · [下一天](day-145.md)

前置：完成 [Day 143](day-143.md) 的验收；已会的内容可直接挑战验收。 GPU 实操需要独占或获准使用的 Linux NVIDIA 环境；没有 GPU 时先做公式、接口模拟与排障推演，实机项保持未完成。

## 今天为什么学这个（20 分钟）

低利用率可能是负载不足、CPU 分词、读取等待、批次太小或采样方式问题。扩显卡前先确认 GPU 是否真是瓶颈。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 固定同一请求集，记录请求到达率、队列、CPU、IO 与 GPU 时间线

2. 逐步增加受控并发，看吞吐是否提升

3. 缓存已加载数据并重复实验，隔离下载与预处理干扰

## 观察结果与验收（10 分钟）

结论基于时间关联与单变量实验，不因看到 10% 就断言模型配置错误。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

故意在客户端请求之间加长 sleep，观察低 GPU 利用率来源于负载生成端。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

为什么服务器空闲时的低利用率不是性能事故？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-144.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-144: 低 GPU 利用率`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-5)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
