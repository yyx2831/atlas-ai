# Day 046：Embedding 的几何表示

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-045.md) · [下一天](day-047.md)

前置：完成 [Day 45](day-045.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

Embedding 把文本映射到向量空间，近邻反映训练得到的相似关系，不是绝对事实。不同模型、维度和归一化策略不能随意混用。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 为三句设备故障和两句无关文本调用同一 embedding 模型

2. 记录模型 ID、维度与预处理版本

3. 实现 embed_text 和批量接口，在索引 metadata 保存 embedding 版本

## 观察结果与验收（10 分钟）

同一 collection 的向量长度一致；能够查看数值而不把维度误解为词数。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

混入另一维度向量，确认写入被拒并能定位模型配置不一致。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

升级 embedding 模型为什么往往需要重建索引，而不是仅改查询模型？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-046.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-046: Embedding 的几何表示`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-2)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
