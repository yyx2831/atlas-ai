# Day 080：条件边与状态路由

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-079.md) · [下一天](day-081.md)

前置：完成 [Day 79](day-079.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

条件边应基于结构化状态决定下个节点，不依赖模型输出一句模糊中文。状态分支要覆盖错误、完成和待审批。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 实现 route_after_agent，分别返回 tools、finish、approval 或 fail

2. 为每个分支写一个最小 State 示例

3. 验证未知状态进入显式错误而不是默认执行工具

## 观察结果与验收（10 分钟）

工具请求走 tools，无工具最终消息走 END，危险动作进入审批且未执行。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

构造空消息列表，确认有明确定义的处理而不是下标越界。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

为什么路由函数适合保持无副作用？如何让分支可测试？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-080.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-080: 条件边与状态路由`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-3)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
