# Day 112：完整 Compose 系统

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-111.md) · [下一天](day-113.md)

前置：完成 [Day 111](day-111.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

多服务系统需要明确依赖图，模型是可选资源 profile，GPU 不足时仍应能运行 API 和 mock 测试。不能让所有服务互相依赖形成环。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 整合 frontend、backend、postgres、redis、qdrant 与 vllm

2. 数据库和模型仅使用内部网络，入口只发布 Nginx

3. 为 mock/CPU 学习与 GPU 实机分 profile，逐个验证依赖

## 观察结果与验收（10 分钟）

通过一次启动完成有条件的全链路，不具备 GPU 的环境可以运行 mock 但不冒充实机验收。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

停 Qdrant 后检查聊天与知识查询的不同降级状态，恢复后不丢索引。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

前端为何不应直接连接数据库或模型服务？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-112.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-112: 完整 Compose 系统`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-4)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
