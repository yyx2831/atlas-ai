# Day 021：让设备 CRUD 真正落库

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-020.md) · [下一天](day-022.md)

前置：完成 [Day 20](day-020.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

接数据库是贯通路由、服务、事务与响应序列化。依赖只在路由解析，service 显式拿 Session；同步 ORM 先配同步端点，避免阻塞事件循环。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 按衔接手册实现数据库版五个 service，显式处理提交与回滚

2. 为五个路由注入 get_db 并传入 service，响应模型启用 from_attributes

3. 连接迁移后的测试 PostgreSQL，完成创建、查改删，再创建一条并重启服务

## 配套完整实验

继续阅读 [数据库衔接实验](../labs/database-bridge.md)，其中有本阶段的完整 SQL、Session 示例、路由接线与复测请求。

## 观察结果与验收（10 分钟）

重启后第二条设备仍存在，删除项返回 404；无 token 仍 401，非法数据仍 422；确认查询的是同一数据库。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

改错测试库端口，确认连接错误被日志定位，恢复配置再验证；不把数据库故障伪装成空列表。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

如果一次业务需要调用两个写入 service，应由谁统一 commit 才能保证原子性？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-021.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-021: 让设备 CRUD 真正落库`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-1)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
