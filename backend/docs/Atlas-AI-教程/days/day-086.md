# Day 086：多工具契约与协议版本

[课程首页](../README.md) · [进度](../PROGRESS.md) · [上一天](day-085.md) · [下一天](day-087.md)

前置：完成 [Day 85](day-085.md) 的验收；已会的内容可直接挑战验收。

## 今天为什么学这个（20 分钟）

工具输入 schema 与结果契约是跨进程接口，变化需要版本管理。规范更新不代表你安装的 SDK 已完整实现每项能力。

把今天的知识放回 Atlas：你要交付的是下面这组能复现的行为，而不只是记住术语。实验使用虚构设备与测试数据，路径以仓库根为基准；Bash 与 PowerShell 的区别见环境说明。

## 跟着完成（70 分钟）

1. 增加 get_alarm 与 search_manual，沿用前面限制与来源结构

2. 保存工具发现结果并检查必填参数、说明和输出

3. 按本地 SDK 与规范兼容说明验证使用能力，不手写不确定的协议字段

## 观察结果与验收（10 分钟）

三个工具可独立调用，错误结构统一；记录 tested SDK version 与传输，而不是只写“最新版”。

保存输入、实际输出、使用的版本和耗时。实际结果不符合预期时，先保留原始错误，再检查本日涉及的配置与数据；不要同时修改多个变量。

## 故障实验（15 分钟）

变更一个必填参数但不更新 client，观察校验失败并补兼容测试。

完成后撤销故障注入，重复正常路径。涉及删除、停服务、断网、OOM 的实验只在可恢复的隔离测试环境执行；没有条件时用模拟错误，不对正在使用的系统操作。

## 独立练习与自测

原计划提及的协议版本如何核对？为什么部署文档需要写实际验证版本？

先写自己的解释，再对照概念段和官方资料。能复述定义但无法解释实验现象，进度仍标为「练习中」。

## 今天做到这里就可以收工（5 分钟）

- [ ] 正常路径通过，并保存可复现证据。
- [ ] 故障原因、定位依据、恢复动作各写一句。
- [ ] 独立回答本日问题。
- [ ] 按 [记录模板](../templates/daily-note.md) 写 `docs/learning/day-086.md`，更新进度。

检查 `git diff`，只暂存今天确认过的文件，手动提交，例如 `day-086: 多工具契约与协议版本`。教程生成不等于学习完成，也不会代你提交业务代码。

## 延伸阅读

[本阶段官方资料与版本说明](../SOURCES.md#month-3)。先完成最小实验，再阅读与当前问题相关的章节，不要求读完整份文档。
